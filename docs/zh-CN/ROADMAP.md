# Lemon Agent 项目路线图

- **阶段 0：项目初始化与技术基线**
  - 建立 Rust 2024 项目结构，划分 `kernel`、`scheduler`、`llm`、`evolution` 等模块。
  - 引入最小必要依赖，并配置格式化、静态检查、单元测试和 CI。
  - 定义统一的错误类型、配置加载机制和结构化日志规范。
  - 提供示例 `config.toml`，支持命令行参数与环境变量覆盖。
  - 完成标准：项目可在目标平台通过 `cargo build`、`cargo test` 和 `cargo clippy`。

- **阶段 1：事件存储与状态恢复（MVP 基础）**
  - 实现 SQLite `events`、`snapshots` 表及必要索引。
  - 定义并序列化全部核心事件类型，包括任务、步骤、LLM、工具、错误、心跳和进化事件。
  - 实现事件追加、按连续性 ID 查询、快照创建和事件重放。
  - 实现启动恢复：加载最新快照并重放后续事件，恢复未完成任务。
  - 增加序列单调性、重复写入、崩溃恢复和损坏数据处理测试。
  - 完成标准：进程在任意状态退出后，重启能够恢复到一致状态并继续执行。

- **阶段 2：能力令牌、安全沙箱与基础工具**
  - 实现能力令牌的资源范围、读写执行权限和可选过期时间校验。
  - 实现沙箱根目录约束、路径规范化和目录穿越防护。
  - 实现 `read_file`、`write_file`、`append_file`、`list_dir` 和 `search_code`。
  - 实现带命令白名单、参数净化、非交互限制和 120 秒超时的 `exec_command`。
  - 实现受限的 `git_add_commit`、`sleep` 和日志工具。
  - 将每次工具调用及其结果写入 `ToolCall` 审计事件，并避免在日志中泄露密钥。
  - 完成标准：越权路径、未授权命令、注入参数和超时操作均被拒绝并留下可审计记录。

- **阶段 3：LLM 网关与上下文管理**
  - 实现 OpenAI 兼容 API 客户端，支持可配置的模型、基础 URL、密钥和温度。
  - 实现指数退避重试、请求超时、错误分类和可选流式响应。
  - 将 Rust 工具定义转换为 Function Calling 所需的 JSON Schema，并解析工具调用结果。
  - 实现消息上下文、滑动窗口、token 估算和超限摘要压缩。
  - 将 `LLMRequest` 与 `LLMResponse` 持久化，同时对敏感信息和过长内容进行处理。
  - 完成标准：可通过模拟服务稳定完成普通对话、工具调用、重试和上下文压缩测试。

- **阶段 4：调度器与端到端任务循环（首个可用版本）**
  - 实现 `Idle`、`Planning`、`Executing`、`Evaluating`、`Evolving`、`Terminated` 状态及合法转换。
  - 实现预算控制：步数、输入 token、LLM 调用、工具调用和墙钟时间上限。
  - 在每步开始前检查预算，并在耗尽时记录原因、生成总结和安全终止。
  - 串联任务输入、LLM 规划、工具执行、结果验证、快照和最终报告。
  - 为异步操作增加超时，为执行步骤增加心跳事件和短暂空转保护。
  - 支持通过命令行初始任务启动，并在完成后返回 `Idle` 等待后续任务。
  - 完成标准：Agent 能在沙箱内自主完成一个小型代码任务，运行测试，并在重启后续接未完成任务。

- **阶段 5：Rhai 脚本引擎与热重载**
  - 启动时扫描并编译 `scripts/*.rhai`，缓存有效 AST。
  - 将受能力令牌和预算约束的文件、命令、Git、LLM、延时及日志工具暴露给 Rhai。
  - 定义脚本入口、参数和 `Result<String, String>` 返回约定。
  - 使用文件监听实现热重载；编译失败时保留上一份可用 AST。
  - 提供 `plan_and_execute.rhai` 示例及脚本级单元测试和集成测试。
  - 完成标准：脚本修改后无需重启即可生效，错误脚本不会破坏当前可用运行时。

- **阶段 6：受控自主进化**
  - 实现可配置的进化触发条件：脚本错误、工具失败或评估结果不佳。
  - 收集连续性 ID、步骤号、错误、脚本源码、最近 10 条消息和最近 5 次工具结果。
  - 构建结构化进化提示，并通过 LLM 生成候选 Rhai 脚本。
  - 实现旧脚本备份、新脚本原子写入、重新编译和隔离验证。
  - 验证失败时自动回滚；成功或失败均记录脚本哈希、原因和进化事件。
  - 将每个连续性的进化尝试限制为默认 5 次，并评估静态危险操作检查。
  - 完成标准：故意注入的脚本缺陷可被修复并通过验证；失败候选始终可可靠回滚。

- **阶段 7：可靠性、安全加固与可观测性**
  - 补齐异常退出、数据库锁、API 限流、网络中断、磁盘写入失败和子进程挂起场景。
  - 验证所有外部操作均经过沙箱和能力校验，不存在绕过路径。
  - 完善结构化日志、心跳、连续性 ID、步骤号、资源消耗和进化结果指标。
  - 增加长时间运行、故障注入、恢复一致性、预算边界和跨平台测试。
  - 对照 E001–E005 错误码统一错误分类、恢复策略和最终报告内容。
  - 完成标准：通过安全审查与 24 小时稳定性测试，故障后无事件丢失或权限扩大。

- **阶段 8：打包、部署与 v1.0 发布**
  - 完善发布模式构建、配置文档、运行手册、审计与恢复指南。
  - 提供多阶段 Dockerfile，打包二进制、默认脚本和最小运行环境。
  - 配置 Docker 重启策略示例，并提供 systemd 部署示例。
  - 验证 Linux、Windows 和 macOS 的构建及核心功能兼容性。
  - 建立版本迁移策略，尤其覆盖 SQLite schema 和脚本接口变更。
  - 完成标准：新环境可按文档部署，并成功完成端到端任务、崩溃恢复和日志审计验收。

- **v1.0 后续路线**
  - 引入多项目工作区隔离和更细粒度的能力令牌。
  - 使用 Tokio 多线程支持受预算约束的并行子任务；分布式执行作为独立长期项目评估。
  - 扩展数据库查询、受控网络请求和 Docker 操作等工具。
  - 自动生成并运行测试套件，提升评估与进化的可靠性。
  - 基于历史事件优化提示和脚本选择，但保持可解释、可回滚和可审计。
  - 在完全隔离的实验环境中探索基于动态库的 Rust 功能热更新，不纳入默认生产能力。

- **全程质量门槛**
  - 每个阶段都必须包含单元测试、关键集成测试和对应文档。
  - 安全边界、事件持久化、预算终止和回滚机制不得作为发布后的补充项。
  - 所有外部副作用必须可追踪，所有长时间操作必须可超时，所有自动变更必须可恢复。
  - 优先完成单机、单任务、脚本级进化的可靠闭环，再扩展并发和高级工具。
